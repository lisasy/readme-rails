development: &default
  adapter: postgresql
  database: readmeapp_development
  encoding: utf8
  host: localhost
  min_messages: warning
  pool: 5
  timeout: 5000

staging:
  adapter: postgresql

production:
  adapter: postgresql

test:
  <<: *default
  database: readmeapp_test
